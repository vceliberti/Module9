plot(yearlytempNewHaven)  ##seeing what the default is

plot(nhtemp)  ##seeing what default is
plot(nhtemp, main = "Yearly Temperature in New Haven", xlab = "Year", ylab = "Temperature in Fahrenheit",
     type = "b", las = 1, col = "sienna", col.main = "darkcyan", lty = "longdash",
     bty = "n", bg = "transparent")
colors()
